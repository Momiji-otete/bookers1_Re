<% if book.errors.any? %>
  <div>
    <!--<h3><%#= pluralize(book.errors.count, 'error') %>!!</h3>-->
    <ul>
      <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>